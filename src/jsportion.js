// function checks if string can be a palindrome
// takes in string as arg
// takes in true or false

function palindrome(word){
    
}

//we're checking for more than 1 odd counting character

//iterate through the string
//get a count of each character
// if there are more than 1 odd counting character than it cant be a palindrome